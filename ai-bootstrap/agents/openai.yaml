interface:
  display_name: "AI Bootstrap"
  short_description: "AI Native Engineering Bootstrap System — 为项目注入 AI 治理体系"
  default_prompt: "Use $ai-bootstrap to bootstrap a new project or inject AI governance into an existing project. Supports detection, confirmed Blueprint selection, stack-aware design-token specs, per-stack official directory layouts, stack-specific run commands, generation, validation, --dry-run preview, and --force for explicit overwrite."
